## Unified .gitignore (single source of truth)

# === Editor / OS ===
.vscode/
.idea/
.DS_Store

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
.cache/

# === Environment & Secrets ===
.env
*.env
*.env.*

# === Logs ===
*.log
debug.log

# === Media (user uploads - not versioned) ===
backend_django/media/

# === Model artifacts (keep only ONNX + JSON + README) ===
backend_django/vision_app/onnx_models/*.pth
backend_django/vision_app/onnx_models/*.pt
backend_django/vision_app/onnx_models/*.pth.tar
backend_django/vision_app/onnx_models/*.ckpt

# === Frontend (CRA / build output) ===
node_modules/
frontend/build/
coverage/
*.pnp
.pnp.js
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# (If you later add a root package.json, review these entries.)
